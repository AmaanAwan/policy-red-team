# ============================================================
# Automated Regulatory Robustness Testing — Dependencies
# ============================================================
# Install: pip install -r requirements.txt
#
# CONSTRAINT: No local inference. No PyTorch. No large model
# downloads. All heavy compute is offloaded to cloud APIs.
# IMPORTANT: Versions are pinned to match the tested environment.
# Unpinned versions cause Cloud Run to resolve incompatible
# package combinations (e.g. fastmcp 4.x breaks with mcp 1.29+).
# ============================================================

# --- Core LlamaIndex Framework ---
llama-index-core

# --- Document Parsing (Cloud API) ---
# Replaces the deprecated `llama-parse` package (EOL May 2026).
llama-cloud>=2.8

# --- Vector Store ---
llama-index-vector-stores-faiss
faiss-cpu

# --- Embeddings (Cloud API) ---
# Google Vertex AI text-embedding-004 (768-dim).
llama-index-embeddings-vertex

# --- Google Auth (explicit — needed for ADC credentials passthrough) ---
google-auth>=2.0.0

# --- MCP Server ---
# PINNED: fastmcp 4.x is incompatible with mcp 1.29+ (missing mcp.shared.session).
# fastmcp 3.4.5 + mcp 1.29.0 is the tested working combination.
fastmcp==3.4.5
mcp==1.29.0

# --- Phase 3: ADK Multi-Agent Orchestration ---
google-adk==2.6.2

# Google Cloud AI Platform SDK.
google-cloud-aiplatform

# Async HTTP client — used by runner.py to health-check MCP SSE server.
httpx

# --- Configuration ---
python-dotenv

# --- Storage & PDF Extraction ---
pypdf
google-cloud-storage

# --- Web Framework (FastAPI) ---
fastapi
uvicorn
python-multipart

# --- Testing ---
pytest

